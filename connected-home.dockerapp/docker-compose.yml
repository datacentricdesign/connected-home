version: "3.6"
services:
    # frontend:
    #     build:
    #         context: ../frontend
    #     #image: datacentricdesign/connected-home
    #     ports:
    #      - "8080:80"
    #     environment:
    #      - NGINX_HOST=localhost
    #      - NGINX_PORT=80
    api:
        build:
            context: ../api
        #image: datacentricdesign/connected-home
        ports:
            - "8081:80"
